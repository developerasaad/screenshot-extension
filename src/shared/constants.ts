/** Auto-dismiss timeout for result dialog (ms). */
export const DIALOG_AUTO_DISMISS_MS = 8_000;

/** Dialog interaction extends life by this amount (ms). */
export const DIALOG_INTERACTION_EXTEND_MS = 5_000;

/** Session ID prefix */
export const SESSION_ID_PREFIX = 'ss-';

/** Extension viewer page path */
export const VIEWER_PAGE = 'viewer/viewer.html';

/** Minimum interval between captureVisibleTab calls (ms). Enforces 2/sec limit. */
export const CAPTURE_INTERVAL_MS = 550;

/** Post-scroll settle delay for lazy content and layout settling (ms). */
export const SCROLL_SETTLE_MS = 120;
